import { StyleSheet } from "react-native";

export const backgroundShadow = StyleSheet.create({
  style: {
    /* Shadow */
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 8,
    },
    shadowOpacity: 0.44,
    shadowRadius: 10.32,

    elevation: 16,
  },
});
